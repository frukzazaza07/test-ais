# Node & Composer dependencies
node_modules
vendor

# Laravel storage & logs
storage/*.log
storage/framework/cache/*
storage/framework/sessions/*
storage/framework/views/*
storage/logs/*

# Local environment files
.env
.env.local
.env.development
.env.production
.env.testing

# Docker files
Dockerfile
docker-compose.yml

# Git files
.git

# IDE & Editor files
.vscode/
.idea/
*.swp
*.swo

# Nginx & PHP logs
nginx/
php.log

public/build
docker/mysql/dbdata/

# Laravel-specific exclusions
public/storage             # Storage symlink (created by php artisan storage:link)
bootstrap/cache/           # Compiled cache files
bootstrap/ssr/           # Compiled ssr
.env                       # Environment file (sensitive, copy separately if needed)
.env.*                    # Other environment files (e.g., .env.testing)
 Homestead.yaml            # Laravel Homestead config
Homestead.json            # Laravel Homestead config

# Node.js/Inertia-specific exclusions
node_modules/              # Node dependencies (installed in Dockerfile)
public/mix-manifest.json  # Legacy Mix manifest
public/hot                # Vite hot reload file
npm-debug.log             # Node debug logs
yarn-error.log            # Yarn error logs
